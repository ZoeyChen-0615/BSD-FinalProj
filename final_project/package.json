{
  "name": "final_project",
  "version": "1.0.0",
  "description": "This repository now contains a no-build browser extension MVP for the `Week 5 Goal` in `PROJECT_PROPOSAL.md`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:panel": "esbuild src/panel/panel.js --bundle --format=esm --platform=browser --outfile=src/panel/panel.bundle.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@clerk/chrome-extension": "^3.1.19",
    "esbuild": "^0.28.0"
  }
}
